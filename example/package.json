{
  "name": "@tpl/example",
  "version": "0.1.0",
  "description": "example",
  "scripts": {
    "start": "tsc --build ./src -w",
    "build": "rimraf ./bin && tsc --build ./src ",
    "clean": "rimraf ./node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@avsb/cli": "^0.1.5",
    "glob": "^8.0.3"
  }
}
